package test;

import java.util.HashMap;

public class B extends A{
	
	public static void growTree(Tree tree) {
		tree.isGrownUp = true;
		new Main().new InnerMain();
		new HashMap<byte[], Double>(5, 5);
	}

}
